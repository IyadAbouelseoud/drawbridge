"""The agent layer.

Claude scoped to exactly the residue the deterministic core cannot decide: narrative,
judgment, and triage. It writes prose and recommendations; it never writes a figure, a
citation, or a claim state.
"""
